do
$body$
declare
  num_users integer;
  num_databases integer;
begin
   SELECT count(*) into num_users FROM pg_user WHERE usename = '{{postgresql_superuser_username}}';
   IF num_users = 0 THEN
      CREATE ROLE {{postgresql_superuser_username}} LOGIN CREATEROLE CREATEDB PASSWORD '{{postgresql_superuser_password}}';
   END IF;
end
$body$
;